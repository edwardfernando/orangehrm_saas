<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>

<div id="wrapper">
	<c:if test="${!empty default_previous_url}">
	<div>
		<a href="${default_previous_url}">
		<< Previous
		</a>
	</div>
	</c:if>
	<c:if test="${!empty default_next_url}">
	<div>
		<a href="${default_next_url}">
		Next >>
		</a>
	</div>
	</c:if>
</div>